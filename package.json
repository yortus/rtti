{
    "name": "rtti",
    "version": "0.0.1",
    "description": "",
    "main": "dist/rtti.min.js",
    "typings": "dist/commonjs/index.d.ts",
    "scripts": {
        "clean": "rimraf dist/",
        "build": "tsc -p src && parcel build src/index.ts --out-file dist/rtti.min.js --no-source-maps",
        "test": "tsc -p test && mocha dist/test/**/*.js",
        "prepare": "symlink-dir . node_modules/rtti"
    },
    "license": "Proprietary",
    "devDependencies": {
        "@types/chai": "^4.1.4",
        "@types/mocha": "^5.2.2",
        "chai": "^4.1.2",
        "mocha": "^5.2.0",
        "parcel-bundler": "^1.12.3",
        "rimraf": "^2.6.2",
        "symlink-dir": "^3.1.0",
        "typescript": "next"
    }
}
